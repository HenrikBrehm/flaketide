---
name: Bug report
about: A reproducible incorrect behaviour in flaketide.
title: 'bug: '
labels: bug
---

## What happened

<!-- A clear, concise description of the bug. -->

## What you expected

<!-- What should have happened instead. -->

## Reproduction

1. flaketide command(s):
   ```
   flaketide ...
   ```
2. Relevant `flaketide.toml` (redact secrets):
   ```toml
   ```
3. Test framework + version: <!-- e.g. cargo-nextest 0.9.x, jest 29.x, pytest 8.x -->

## Environment

- flaketide version (`flaketide --version`): 
- OS + arch: 
- Rust toolchain (if built from source): 
- Installed via: `cargo install` / prebuilt binary / Docker / source

## Logs

<!-- Re-run with `-vv` and paste the relevant excerpt (redact ANTHROPIC_API_KEY / GITHUB_TOKEN). -->

```
```
